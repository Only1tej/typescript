function chatFeed(): never {
  while (true) {
    //Grab the messages and display them on the screen
  }
}

// chatFeed() //DOnot run this, it will crash the software
// console.log("Chat disconnected"); //Unreachable code
