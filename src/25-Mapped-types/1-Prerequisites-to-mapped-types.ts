// //------------------------------------------------1. Map method
// console.log([1, 2, 3, 4, 5].map((item) => item.toString()));

// //-----------------------------------2. Index access type
// type Product = {
//   name: string;
//   price: number;
// };

// type CourseName = Product['name']
