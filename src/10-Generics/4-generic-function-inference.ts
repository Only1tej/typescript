//Type inference doesn't eliminate type safety

// function getArrayLength<T>(elements: T[]): number {
//   return elements.length;
// }

// let arrLength = getArrayLength([1, 2, 3, 4]);
// console.log(arrLength);
